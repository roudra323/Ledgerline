# Ledgerline Project Rules

This file is a pointer, not a rulebook. It deliberately contains **no rules of its own** — an agent
that reads a paraphrase instead of the source follows a lossy copy and believes it complied.

Read both of these, in full:

- [`../../CLAUDE.md`](../../CLAUDE.md) — golden rules, where facts live, working rhythm, definition of done
- [`../../docs/conventions.md`](../../docs/conventions.md) — the coding contract

<!-- ssot:pointer-only — `pnpm docs:check` fails if this file grows rule text -->
