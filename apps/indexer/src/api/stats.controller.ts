/**
 * StatsController — GET /stats.
 *
 * TODO(Part 6): payment volume, merchant count, total issued (from the ledger, not the chain),
 * and the reconciliation summary. Served from projections; the chain is never called on a read
 * path.
 */

export {};
