/**
 * HealthController — GET /health/indexer: cursor positions, chain head, lag (blocks & seconds).
 *
 * TODO(Phase 1): surface eventual consistency honestly — this feeds the frontend "lag" badge.
 * TODO(Phase 5): shape response as { block, lagBlocks, lagSeconds, syncKeys: [...] }.
 */

export {};
