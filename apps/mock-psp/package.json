{
  "name": "@ledgerline/mock-psp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A deterministic fake payment provider with a fault-injection API. The chaos harness for the fiat rail.",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "node --test"
  },
  "dependencies": {
    "@ledgerline/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3"
  }
}
