name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  PNPM_VERSION: 10.26.0
  NODE_VERSION: 22

jobs:
  lint:
    name: Lint & typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      # `pnpm typecheck` fans out to every workspace, and @ledgerline/contracts typechecks by
      # compiling Solidity. Without Foundry on PATH this job fails at the contracts package.
      - uses: foundry-rs/foundry-toolchain@v1
      - run: forge install --no-git foundry-rs/forge-std OpenZeppelin/openzeppelin-contracts
        working-directory: packages/contracts
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm format:check

  contracts:
    name: Foundry tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: foundry-rs/foundry-toolchain@v1
      # Foundry deps live in lib/ (git-ignored) and are installed here, not committed.
      # Pin versions once they're chosen in Phase 0 (e.g. --no-commit @<tag>).
      - name: Install contract dependencies
        working-directory: packages/contracts
        run: |
          forge install foundry-rs/forge-std
          forge install OpenZeppelin/openzeppelin-contracts
      - name: forge build
        working-directory: packages/contracts
        run: forge build --sizes
      - name: forge test
        working-directory: packages/contracts
        run: forge test -vvv
      # TODO(Phase 7): add `forge test --match-test invariant` and optional slither job.

  unit:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test
      # TODO(Phase 7): idempotency + replay-golden tests.

  integration:
    name: Integration (Anvil + Postgres)
    runs-on: ubuntu-latest
    # TODO(Phase 7): spin up testcontainers/compose (Anvil + Postgres),
    # fire real txs, assert projections, kill/restart mid-stream, simulate reorg.
    steps:
      - uses: actions/checkout@v4
      - run: echo "Integration tests are wired up in Phase 7."

  docker-build:
    name: Docker build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build indexer image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/indexer/Dockerfile
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
