{
  "name": "@ledgerline/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared ABIs, types, and deployed contract addresses for Ledgerline.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./abis": "./src/abis/index.ts",
    "./addresses": "./src/addresses.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "abi:gen": "echo 'TODO(Phase 0): generate ABIs from packages/contracts/out into src/abis'"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
