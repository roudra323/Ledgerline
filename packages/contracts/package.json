{
  "name": "@chainstake/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "ChainStake Solidity contracts (Foundry): StakingVault + MockToken.",
  "license": "MIT",
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "test:invariant": "forge test --match-test invariant -vvv",
    "fmt": "forge fmt",
    "fmt:check": "forge fmt --check",
    "deploy:local": "forge script script/Deploy.s.sol --rpc-url http://localhost:8545 --broadcast",
    "typecheck": "forge build"
  }
}
