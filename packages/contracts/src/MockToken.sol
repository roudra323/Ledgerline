// SPDX-License-Identifier: MIT
pragma solidity ^0.8.28;

// TODO(Phase 0): `forge install OpenZeppelin/openzeppelin-contracts` then:
// import {ERC20} from "@openzeppelin/contracts/token/ERC20/ERC20.sol";

/// @title MockToken
/// @notice Simple ERC20 used as the staking asset in local/dev environments.
/// @dev Includes an open `mint` so the loadgen and tests can fund accounts freely.
contract MockToken {
    // TODO(Phase 0): extend ERC20("ChainStake Mock", "CSM"); add mint(address,uint256).

    }
